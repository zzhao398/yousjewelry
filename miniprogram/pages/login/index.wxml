<view class="page">

<!-- 右上角 language 语言 -->
<view class="lang-switcher">
  <view class="lang-btn" bindtap="onLangButtonTap">
    <text>language 语言</text>
  </view>

  <view wx:if="{{langMenuVisible}}" class="lang-menu">
    <view class="lang-item" data-lang="zh" bindtap="onSelectLang">
      <text>{{tCommon.lang_zh}}</text>
    </view>
    <view class="lang-item" data-lang="en" bindtap="onSelectLang">
      <text>{{tCommon.lang_en}}</text>
    </view>
  </view>
</view>

<!-- 顶部标题 -->
<view class="logo-area">
  <text class="logo">{{tLogin.logo}}</text>
</view>

<!-- 登录表单 -->
<view class="form">
  <view class="input-row">
    <view class="icon">👤</view>
    <input
      placeholder="{{tLogin.account_placeholder}}"
      value="{{loginAccount}}"
      data-field="loginAccount"
      bindinput="onInput"
    />
  </view>

  <view class="input-row">
    <view class="icon">🔒</view>
    <input
      placeholder="{{tLogin.password_placeholder}}"
      password="true"
      value="{{password}}"
      data-field="password"
      bindinput="onInput"
    />
  </view>

  <button
    class="btn-login"
    type="primary"
    loading="{{loading}}"
    bindtap="onLoginTap"
  >
    {{tLogin.btn_login}}
  </button>
</view>

<!-- 注册入口 -->
<view class="register-area">
  <button class="btn-register" bindtap="onRegisterAnchor">
    {{tLogin.btn_register_anchor}}
  </button>
  <button class="btn-register" bindtap="onRegisterAdmin">
    {{tLogin.btn_register_admin}}
  </button>
</view>

<view class="footer">
  <text>{{tLogin.footer_copyright}}</text>
</view>
</view>
